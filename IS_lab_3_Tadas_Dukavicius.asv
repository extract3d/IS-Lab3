x = 0.1: 1/22: 1;
for i=1:length(x)
    d(i) = (1 + 0.6*sin(2*pi*x(i)/0.7) + 0.3*sin(2*pi*x(i)))/2;
end

c1 = randn(1);
c2 = randn(1);
r1 = randn(1);
r2 = randn(1);

w1 = randn(1);
w2 = randn(1);
b = randn(1):

for i=1:i
    F 